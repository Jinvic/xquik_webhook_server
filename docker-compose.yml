services:
  xquik-webhook:
    image: jinvic/xquik-webhook-server:latest
    # build:
    #   context: .
    #   dockerfile: Dockerfile
    container_name: xquik-webhook-server
    restart: unless-stopped
    ports:
      - "${HTTP_PORT:-8080}:8080"
    env_file:
      - .env
